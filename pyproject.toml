[project]
name = "ethical-twin-openenv"
version = "0.1.0"
description = "Ethical Twin OpenEnv environment for reinforcement learning clinical trial simulation"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv-core>=0.2.0",
]

[project.scripts]
server = "server.app:main"
